/** Atmósfera extra, SOLO para la galería (/app) — pinta por encima del Aurora
 * global pero por debajo del contenido real de la página. CSS puro. */
export default function LibraryAurora() {
  return <div className="lib-aurora" aria-hidden="true" />;
}
